# Deployment 3: Replica count: 1; containers per pod: 1; lim defined only
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-1-1-lim
  namespace: validakr-test-workloads
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deploy-1-1-lim
  template:
    metadata:
      labels:
        app: deploy-1-1-lim
    spec:
      containers:
      - name: container
        image: docker.io/sandipandevzero/dakr-stress-single:latest
        imagePullPolicy: Always
        env:
        - name: CPU_CORES
          value: "4"
        - name: CPU_LOAD
          value: "30"
        - name: CPU_DURATION
          value: "180"
        - name: RAM_MB
          value: "2500"
        - name: RAM_DURATION
          value: "180"
        - name: SLEEP_MIN
          value: "120"
        - name: SLEEP_MAX
          value: "6000"
        resources:
          limits:
            cpu: 4000m
            memory: 4Gi
